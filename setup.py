from setuptools import setup, find_packages

setup(name='i2i',
      version='0.0.1',
      description='Tools to build adapters for one interface to talk to another',
      url='http://github.com/thorwhalen/i2i.git',
      author='OtoSense',
      license='Apache',
      packages=find_packages())
